---
- name: Setup the Ansible controller
  hosts: "{{ target_host }}"
  gather_facts: no
  vars:
    remote_user: friasc
    ansible_workspace: "~/apigee-workspace"

  roles:
  - { role: apigee-opdk-setup-ansible-controller }
