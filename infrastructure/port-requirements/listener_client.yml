---

- name: Setup listener
  shell: "nc -l -p {{ port }} &"

- name: Validate port
  wait_for:
    port: "{{ port }}"
    timeout: 2
    host: "{{ private_address }}"
  delegate_to: "{{ hostvars[item]['private_address'] }}"
  with_items: "{{ groups['ds'] }}"

